import { Router } from 'express'

// Placeholder. Registration, login, and JWT issuance land in Phase 2 — this
// router exists so the route surface is in place and is intentionally not
// mounted in index.ts until it has real handlers.
export const authRouter = Router()
